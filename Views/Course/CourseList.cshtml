@model List<Course>

<main>
    <h1>Course List</h1>
    @foreach(var kurs in Model)
    {
        @if(kurs.isActive){
            @await Html.PartialAsync("_CourseCard", kurs)
        }
        
    }
</main>
